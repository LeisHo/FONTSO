FONTSO -- CHANGELOG
================================================================================

PROJECT START: 2026-09-22

--------------------------------------------------------------------------------
2026-09-22  -  4:31 AM - 4:38 AM

Created FONTSO as a blank HTML project scaffold: the workspace-standard dev
panel (CLAUDE.md S12) and UI Layout Engine (S13), wired together and verified
working, with no application features. The folder already existed and was a
copy of HANDYSET -- 150,899 bytes of three.js hand-rig code in src/main.js,
HANDYSET's own 42,997-byte CLAUDE.md, and its filled-in docs. All 10 of those
inherited files were MOVED to data/raw/inherited-handyset-2026-09-22/, not
deleted (CLAUDE.md S11: "nothing gets deleted by default"); that folder is
gitignored and nothing live reads it.

Added:
- src/devpanel/devPanel.js -- the <script> block of
  .claude/TEMPLATE_DEV_PANEL.html, copied verbatim, differing by exactly one
  inserted line (plus a 5-line comment explaining it) at the template's own
  documented [JS-14] splice point: `if (window.renderFontsoDevGroups)
  window.renderFontsoDevGroups();`. 4,570 lines.
- src/style.css -- the template's <style> block verbatim (877 lines, Part 1),
  then this project's own page styling and the host-side chrome for the UI
  Layout Inspector (Part 2). A banner comment marks the boundary.
- index.html -- the template's <body> markup and its early html.dev-mode
  <head> script, both verbatim, plus the #appStage root, the two stylesheet
  links, and the two deferred scripts. Script order is load-bearing and
  documented in its own <head> comment: src/main.js (a module) must run
  before src/devpanel/devPanel.js (a classic script given `defer` so it joins
  the same deferred queue), or devPanel.js's eager ensureDevPanelBuilt() call
  finds no window.renderFontsoDevGroups to invoke.
- lib/ui-engine/ -- UI Layout Engine v0.2.6 vendored from
  J:\CLAUDE\PROJECTS\HTML UI ENGINE\. All 11 files (9 .mjs + engine.css +
  tokens.json) confirmed byte-identical to the canonical source via `cmp`.
- src/main.js -- the host half of the ENGINE_API.md contract. Registers
  #appStage from git-tracked intent in data/processed/ui-layout-config.json;
  bridges the dev panel's Desktop/Mobile/Landscape device rule to the
  engine's base/mobilePortrait/mobileLandscape contexts via
  setActiveContextOverride(), so the two can never disagree about which
  context is active (the mismatch DEV_PANEL_ADAPTER.md S13.1 documents);
  mounts the engine's Inspector as a panel-level "UI Layout" group; and adds
  the optional S12l git-tracked persistence on top of localStorage.
- addRow() in src/main.js -- the single choke point every future control
  passes through. It sets ctrl.tab='desktop' and collects the control for
  registerDevControlArray(), closing the two silent-failure traps the shared
  template warns about (a wrong per-row device checkbox, and a
  "Show in Mobile/Landscape" checkbox that toggles but creates no row).
  Zero project controls exist yet -- this is wiring for the first one.
- api/save-settings.js -- the S12l Vercel write-through, generalized from the
  inherited copy. GITHUB_REPO now has NO hardcoded default (this project has
  no repo yet; a wrong default would fail confusingly or write somewhere
  unintended). Inert until its env vars and the client-side secret are set;
  localStorage carries everything until then.
- scripts/active/serve.py -- local dev server, adapted from the engine's own
  demo/serve.py. Required, not optional: `python -m http.server` sends .mjs
  as text/plain and browsers reject that for <script type="module">, so every
  engine import fails. Also sends Cache-Control: no-store.
- CLAUDE.md, README.md, and the CLAUDE.md S11 folder skeleton (src/, lib/,
  data/processed + data/raw, scripts/active + scripts/archive, docs/, api/).
- docs/PROJECT_SUMMARY.txt, CODE_SUMMARY.txt, PROJECT_PROGRESS.md, HANDOFF.md
  and this file, generated from the .claude/TEMPLATE_* standards and left as
  BLANK templates per direct instruction. The scaffold's own technical facts
  (file map, the verbatim-copy rule, 8 inherited gotchas, the deliberate
  architecture exceptions) live in CLAUDE.md instead, so nothing is lost by
  those four staying empty until FONTSO has a purpose.

Decisions:
- The "UI Layout" Inspector group is PANEL-LEVEL, not one copy per tab --
  beside "Saved Dev Settings", outside all three tab-content roots. Same
  precedent S12d sets for the same reason: the Inspector carries its own
  Base/Mobile Portrait/Mobile Landscape context tabs, so one instance already
  covers all three. It would also mirror into the other tabs as an empty
  shell if it lived inside them, since cross-tab mirroring can only copy
  registered control rows (the same reason the built-in "Mouse Log" is
  excluded from mirroring by name).
- HANDYSET's per-group cascade RETENTION feature was deliberately NOT carried
  over. It exists only in that project's own copy of devPanel.js and was
  never folded back into .claude/TEMPLATE_DEV_PANEL.html; FONTSO tracks the
  canonical template. If that behaviour is wanted here, it should go into the
  template first, then be re-copied.
- remotePutSettings() does GET -> merge -> POST, never a blind POST. The
  settings file holds several independent top-level keys (devPanel,
  uiLayoutConfig) written by different code paths; a blind overwrite from
  either silently erases the other.

Verified (live, http://localhost:8420, real browser):
- All 11 engine modules, both scripts, both stylesheets and the layout config
  return 200 OK. Zero JavaScript console errors. The only 404s are
  /favicon.ico and /api/save-settings -- the latter is the expected,
  silently-handled localStorage fallback path with no serverless runtime
  running locally.
- Panel chrome renders in full: 8 header icon buttons, COPY/RESET/SYNC,
  Search, the "Saved Dev Settings" group with SAVE/USE/DELETE/SET DEFAULT,
  the Desktop/Mobile/Landscape tabs, and both mandatory built-in groups
  ("Dev Panel" with its 3 subgroups, "Debug" with Mouse Log).
- Both template CSS fixes are present and working: the group cascade checkbox
  computes to display:block with real width (not the old hardcoded
  display:none), and .dev-section-title computes to position:relative (so
  that checkbox anchors to the title bar, not the whole group).
- S12b, the specific silent-failure the template demands a LIVE check for: a
  temporary in-page probe control added exactly the way addRow() adds one got
  the correct checkbox kind ("Show in Mobile/Landscape", not "Independent
  from Desktop"), and checking it genuinely created sliderMobileProbe and
  sliderLandscapeProbe rows. Probe was in-page only; nothing written to disk,
  and a reload confirmed it gone.
- Engine end-to-end: the Inspector's element picker showed the 2-level
  Object/Property cascade ("App Stage" -> "Position & Size"), all 3 context
  tabs, Position/Size-Width/Size-Height sections (Constraints correctly
  absent -- that section was never configured for this element), all 5
  position and 6 size modes. Editing offsetX from 0px to 40px moved
  #appStage's computed `left` from 0px to 40px; reverting returned it to 0px.

Not verified:
- The api/save-settings.js write-through against a real Vercel deployment --
  no deployment, no repo, and neither secret configured yet. Only its local
  absence (the localStorage fallback) was exercised.

--------------------------------------------------------------------------------
2026-09-22  -  4:44 AM - 5:00 AM

Built the Font Path Laboratory (lab/): a browser feasibility prototype
testing whether arbitrary font glyphs can be converted automatically into
usable drawable centreline paths. No hand-authored letterforms, no stroke
font, no per-character tables -- every skeleton is derived at runtime from
the supplied font's own outline. Ten single-responsibility modules; only
pipeline.mjs knows the stage order.

Pipeline: glyph outline (opentype.js) -> canvas-filled binary mask ->
Zhang-Suen / Guo-Hall thinning -> crossing-number graph (nodes/edges/
components) -> prune+simplify+smooth cleanup -> path segments in both
raster px and font units -> deterministic traversal with explicit pen-up
connectors -> layered canvas visualiser with a constant-speed animator.

Added:
- lab/src/{config,fontLoader,glyphExtract,rasterize,skeletonize,
  skeletonGraph,cleanup,vectorize,traversal,pipeline}.mjs plus
  viz/{renderer,animator}.mjs and app.mjs.
- lab/lib/opentype.mjs -- opentype.js 2.0.0 (MIT), vendored MINIFIED
  build. The 494KB unminified build was intermittently truncated by the
  local static server (200 OK alongside net::ERR_CONNECTION_RESET),
  silently breaking the entire module graph; 245KB is served reliably.
- lab/README.md, lab/test-fonts/manifest.json (font files gitignored).
- .gitignore rules for test fonts and node_modules.

Fixed (all five found by measurement during live testing, not by review):
- Pixel classification used raw 8-neighbour COUNT instead of crossing
  number. A diagonal staircase corner has 3 neighbours but is not a
  junction, so Comic Sans 'O' reported 23 junctions (it has none) and 'H'
  reported 3 endpoints (it has 4). Since glyphs are mostly curves, nearly
  every diagonal pixel was misclassified.
- Edge tracing picked "any neighbour that isn't the previous one", which
  doubles back at a staircase corner. Now prefers a neighbour not
  8-adjacent to the previous pixel.
- Redundant-pixel removal tested ring transitions (T==1), which misses
  the common corner case: ring order says N and W are far apart, but they
  physically touch. Now counts true connected groups among the neighbours.
  This eliminated a phantom 'loop-anchor' node and bogus 1px edge that
  every glyph tested was carrying.
- Tiny-component filtering measured SKELETON LENGTH, which destroyed the
  dot of '?', '!', ':' and 'i' -- measured at 3.4px, 3.8px, 2.4px/5.8px
  and a single pixel respectively, indistinguishable from noise by that
  measure. Now measures SOURCE MASK AREA, where a real dot is hundreds of
  pixels and a speck is single digits. An intermediate attempt that
  deleted tiny self-loops' PIXELS was also wrong and reverted: it severed
  Comic Sans 'H' (4 endpoints/2 junctions/1 component -> 5/1/2), because
  those pixels are the junction's connective tissue. The edge is noise;
  the ink is not.
- describeFont() read font.names.fontFamily flat, but opentype.js v2
  nests names under names.windows / names.macintosh, so every font
  displayed its filename instead of its name.
- lab.css hardcoded `height: calc(100% - 41px)` for the header; the
  header wraps, so content overflowed (755px in a 720px viewport).
  Replaced with flex.
- loadFontFromUrl() now retries 3x: Arial (1.0MB) and Gabriola (1.8MB)
  intermittently failed to load, Gabriola needing 3 attempts in one run.

Verified (live, real browser, real fonts):
- 8 fonts x 8 characters (A H S O B g & ?) = 64 runs, zero errors, zero
  incomplete traversals: Arial, Times New Roman, Georgia, Courier Std
  (OTF/CFF cubic), Comic Sans MS, Ink Free, Segoe Script, Gabriola.
- Topology is exact where it can be checked by hand: H = 4 endpoints /
  2 junctions / 5 edges / 1 component; O = 0/0/1 loop/1 edge; S = 2
  endpoints / 1 edge; B = 0 endpoints / 2 junctions / 3 edges. Zero
  orphaned pixels on every glyph tested.
- Font-specificity confirmed by edge count on the SAME letter: Comic Sans
  'H' = 5 segments, Times/Georgia/Courier 'H' = 13, because the serif
  faces' skeletons branch into every serif. Screenshot-confirmed.
- Animation advances at constant speed and wraps correctly (242px ->
  79.5px across a 316.5px route at 220px/s over 0.7s).
- Per-glyph pipeline cost 1.7ms to 77ms at the 256px default.

Not verified:
- Non-Latin scripts, variable-font instances, colour/bitmap fonts.
- Hairline faces at low rasterEmHeight (expected to fragment; untested).

--------------------------------------------------------------------------------
2026-09-22  -  5:03 AM - 5:07 AM

Follow-up to the entry above, recorded separately rather than editing it
(CHANGELOG is append-only): fixed the local server flake properly instead of
leaving it as a documented annoyance.

Fixed:
- scripts/active/serve.py repeatedly failed to deliver lab/lib/opentype.mjs
  (net::ERR_CONNECTION_RESET with no status), which silently broke the entire
  ES module graph -- the page loaded, the canvas rendered its placeholder, and
  window.fontLab was undefined, with no error pointing at the cause. It hit
  the largest file every time and survived shrinking that file from 494KB to
  245KB, so size alone was not the trigger. Two changes: protocol_version is
  now HTTP/1.1 (the default HTTP/1.0 closes the socket after every response,
  and a page load requests ~20 files -- that connection churn was the real
  cause), and /lib/ is served cacheable (max-age=3600) while project code
  stays no-store, so the large vendored bundle is transferred once rather
  than on every reload. Also swallows ConnectionAborted/Reset/BrokenPipe in
  copyfile (normal when a browser navigates away mid-transfer) and quietens
  per-request logging to errors only.

Verified:
- 5 consecutive cold page loads, all with window.fontLab defined. Before the
  fix the failure recurred within 2 consecutive attempts.
- Comic Sans 'B' end to end after the fix: 3 segments, 1 component, 40ms,
  12-row pipeline report, 9,128 characters of debug JSON.
- Animator confirmed advancing (0 -> 20.4px of a 569.9px route across 19
  animation frames). An earlier reading of 0 was the sandbox's own
  requestAnimationFrame throttling on an unobserved browser-pane tab, not an
  application defect -- frames only advance while the tab is actually being
  rendered, which is a known property of this environment.

--------------------------------------------------------------------------------
2026-09-22  -  5:30 AM - 5:35 AM

Migrated the Font Path Laboratory onto the workspace-standard dev panel
(CLAUDE.md S12), and added a new "Path" group of rendering controls
requested via S12g shorthand.

Added:
- lab/devpanel.js and lab/devpanel.css -- the <script> and <style> blocks of
  .claude/TEMPLATE_DEV_PANEL.html, copied VERBATIM, the script differing by
  exactly one inserted line at the template's own [JS-14] splice point.
  This is a SECOND independent copy alongside the root scaffold's in src/ --
  deliberate, so lab/ stays liftable into another project as a whole.
- 8 laboratory groups registered into the panel from src/app.mjs: Font &
  Character, Layers, Path, Animation, Rasterisation, Skeletonisation,
  Cleanup, Traversal. All 17 config.mjs parameters are now exposed with
  units inline in the label (S12n); the old bespoke side panel surfaced only
  6, so the other 11 could previously only be changed by editing source.
- Pipeline Report and Path Data (JSON) as nested subgroups of the mandatory
  built-in Debug group (S12i-1), rather than inventing a top-level group.
- Hand-built widget rows (file input, test-character buttons, play/restart,
  status lines, report, JSON) marked data-skip-device-checkbox, the same way
  the built-in Mouse Log widget is -- single-instance widgets with no
  meaningful Mobile/Landscape counterpart.
- NEW "Path" group (4 controls, per direct request):
    Path Thickness On/Off        - renders the centreline as a real stroke
    Path Thickness (Px)          - 1-40, default 8
    Path Color                   - colour picker, default the skeleton green
    Progressive Thickness On/Off - thickness applies only to the portion the
                                   animated dot has already travelled
  Range and default for thickness were CHOSEN, not specified: 1px is the
  thinnest visible stroke and ~40px is roughly a Comic Sans stem at the
  256px default raster, past which the path stops reading as a path.
- renderer.mjs gains DEFAULT_VIEW + setViewSettings(). These are RENDER-ONLY
  settings, deliberately NOT in config.mjs: that file is the pipeline's
  parameter set and every change to it re-runs the whole glyph->skeleton
  pass. A colour change must not cost a re-run.

Removed:
- The bespoke side panel, top bar, and ~120 lines of lab.css. The canvas is
  now full-bleed with the dev panel floating over it. lab.css keeps only the
  canvas, the legend, and the hand-built widgets -- the latter styled from
  the panel's OWN custom properties so they follow whatever the built-in
  "Dev Panel" group is set to instead of being separately themed.

Decisions:
- Config controls are wired by ONE DELEGATED listener on #devPanel, not per
  element. Per-element wiring reaches Desktop controls only; the panel
  creates a Mobile/Landscape twin on demand when its checkbox is ticked,
  long after wire-up, so a twin would move, show a new value and drive
  nothing at all -- silently, with no error. Delegation plus the template's
  own resolveDevControlId() wires every twin, present or future, for free.
- All three tabs drive the SAME config value on purpose. Pipeline parameters
  are not device-specific: a glyph's skeleton does not depend on the
  viewport it is inspected in. S12f's per-device independence is for an
  app's spatial styling, which this is not.
- Progressive inking is drawn by drawCleanSkeleton(), not drawDot(), so it
  survives turning the "Animated Dot" layer off -- the inked path is a
  property of the path, not of the dot marker. The dot's own faint wake is
  suppressed while progressive inking is active, since stacking the two
  muddies the chosen colour.
- Path thickness is scaled by the view transform, so the panel's number
  means raster pixels, not screen pixels; otherwise the stroke would stay a
  constant screen width while the glyph zoomed underneath it.

Verified (live, real browser, driving the REAL panel controls):
- 10 groups present (2 built-in + 8 laboratory), 87 rows, clean load.
- S12b, the silent failure the template demands a live check for: ticking
  "Show in Mobile/Landscape" on sliderMinBranchLengthPx genuinely created
  sliderMobileMinBranchLengthPx and sliderLandscapeMinBranchLengthPx. Zero
  hand-built rows were wrongly given a device checkbox.
- The Mobile twin genuinely drives the pipeline: editing
  sliderMobileSimplifyTolerancePx moved config 0.8 -> 3.5 and re-ran the
  pipeline (68 -> 14 polyline points); editing the Desktop control back
  synced the twin to match.
- Panel self-styling works (accent colour #005f8f -> #ff8800 applied to the
  live CSS custom property). Sync persists 44,026 bytes to localStorage.
- Full Comic Sans sweep through the real character buttons after migration
  is IDENTICAL to pre-migration: A 6e/1c, H 5e/1c, S 1e/1c, O 1e/1c,
  B 3e/1c, g 2e/1c, & 7e/1c, ? 2e/2c. No regression.
- Path group: thickness 16px + colour #ff3366 renders the S centreline as a
  thick pink stroke inside the glyph; progressive mode confirmed across 3
  real animation frames, inking in from the top while the remainder stays a
  thin centreline (21.7% progress, 46 trail points).

Testing note worth keeping:
- Delegated wiring cannot be tested with `new Event('change')` -- it does
  not bubble, never reaches the panel, and produces a false negative
  indistinguishable from broken code. It cost one wrong diagnosis here
  before being spotted. Use `new Event('change', {bubbles: true})`. Same
  family as the documented `.click()`-vs-pointerdown trap.

--------------------------------------------------------------------------------
2026-09-22  -  5:47 AM - 5:50 AM

Added the centreline->outline tween (new "Path Tween" dev-panel group) and
an Animation Path selector, both requested via S12g shorthand.

Interpretation, since it drove the whole design: "a tween between the
produced centre line and the glyph outline" means TWO curves per skeleton
edge (hence the plural), offset perpendicular to the path by
progression x local half-thickness. At progression 0 both collapse onto the
centreline; at 1 they land on the outline.

Added:
- lab/src/distanceTransform.mjs -- exact Euclidean distance transform,
  Felzenszwalb & Huttenlocher's linear-time separable algorithm. This
  supplies the local half-thickness: the distance from a skeleton pixel to
  the nearest background pixel IS the inscribed-circle radius there, i.e.
  exactly how far the centreline must move to reach the outline. Nothing
  else in the pipeline can provide it. An approximate chamfer DT was
  rejected deliberately -- its error is direction-dependent, so it would
  read on screen as the tween drifting off the outline on diagonals but
  not on stems: visible, and impossible to tune away. Also provides a
  bilinear sampler, because the skeleton's own points are fractional
  (junction centroids, simplified/smoothed polylines) and nearest-neighbour
  sampling of them produces a radius that steps in 1px jumps, visible as a
  scalloped tween edge.
- lab/src/tween.mjs -- the morph itself: per-point normals (central
  difference, wrapping on closed loops), normal smoothing with
  re-normalisation, radius smoothing, optional resampling and terminal
  extension, plus tweenAnimationRoute() which flattens the tween curves
  into the {flat, cumulative, totalLength} shape the animator consumes.
- NEW "Path Tween" group (8 controls):
    Path Tween On/Off
    Tween Progression (0-1)     - the headline control
    Radius Scale (X)            - corrects the systematic under-reach
    Radius Smoothing (Passes)   - damps the junction spike
    Normal Smoothing (Passes)   - stops the offset wobbling
    Resample Spacing (Px)       - RDP leaves 2-point runs that cannot
                                  follow a changing radius
    Extend Terminals (Px)       - compensates thinning's end erosion
    Both Sides On/Off
- Animation Path dropdown in the Animation group: Midline (skeleton) or
  Tween path. Falls back to the midline whenever the tween is off or
  empty, rather than leaving the animator with no route -- a dropdown set
  to Tween with the tween disabled would otherwise stop the dot dead.
  Preserves fractional position across a switch so the dot does not snap
  back to the start.
- "Tween Curves" layer toggle.

Decisions:
- The tween is RENDER-ONLY. The distance field is computed once per
  pipeline run (5.2ms) and handed out on the result; tween controls
  re-offset the cached polylines (O(points)) and redraw. Putting them in
  config.mjs would have made every slider tick a full re-rasterise and
  re-thin.
- Progression 1 is exact only where the normal genuinely points at the
  nearest boundary -- true along any ordinary stroke, false at terminals
  (thinning erodes ends inward), junctions (the medial axis is genuinely
  ambiguous; the inscribed circle exceeds either stroke's half-width) and
  sharp corners (the offset self-intersects on the inner side). This is
  documented rather than hidden, and three of the fine-tuning controls
  exist specifically for the first two. Corner self-intersection is left
  visible on purpose: in a laboratory that is more informative than a
  silently clipped curve.
- Normal smoothing re-normalises after averaging. Skipping that would
  shorten the vector wherever neighbouring normals disagree, quietly
  reducing the offset exactly at the corners where it matters most.

Verified (live, real browser, driving the REAL panel controls):
- Distance transform sanity on Comic Sans H at 256px: 12.00px at a stem
  centre against a ~24px stem (exactly half), max 15.62px at the
  stem/crossbar junction -- the predicted inscribed-circle overshoot.
  5.2ms for a 173x213 field.
- Offset is exactly linear in progression: median offset 0.00 / 3.00 /
  6.00 / 12.00 at progression 0 / 0.25 / 0.5 / 1, against a 12.00px
  median radius. Progression 0 gives exactly zero offset.
- Screenshots at 0.5 (curves halfway between centreline and outline, both
  sides of every stroke) and at 1.0 with the skeleton layer off (curves
  tracing the glyph edges; the visible gap at the rounded terminals is the
  documented thinning erosion).
- Every fine-tuning control measurably does what it claims: Radius Scale
  1.0->1.15 moved max offset 13.60->15.64 (exactly x1.15); Extend
  Terminals 0->8 added one point per free end (45->47); Resample 2->6px
  took 45 points to 16; Both Sides off nulls the right curve.
- Animation Path: midline route 625.6px, tween route 1721px, switching
  back returns 625.6px. On the tween route the dot measures 0.3-1.6px
  from a tween curve and 10-12.7px from the midline (about the stem
  half-width) across four sampled fractions -- it is genuinely riding the
  tween geometry, not the midline.

--------------------------------------------------------------------------------
2026-09-22  -  5:51 AM - 5:52 AM

Initialised git and pushed the project to GitHub as a PRIVATE repository:
https://github.com/LeisHo/FONTSO (branch main, commit ddee500, 51 files,
21,101 insertions).

Decisions:
- PRIVATE, not public. The request was "create a repo and push", which does
  not by itself mean publish; public is the hard-to-undo direction (content
  is cached and indexed even after deletion) and private -> public is one
  click later. Stated to the user rather than assumed silently.
- A real pre-push audit was run against the ACTUAL staged file list rather
  than trusting .gitignore, because this project references keyps.txt,
  GITHUB_TOKEN and DEV_PANEL_SAVE_SECRET, and lab/test-fonts/ holds
  Microsoft OS fonts that are not redistributable.

Audit results (all clean, checked before the repo was created):
- No token-shaped strings anywhere in the staged set (gh[pousr]_*,
  github_pat_*, AKIA*, PRIVATE KEY blocks).
- GITHUB_TOKEN / DEV_PANEL_SAVE_SECRET appear only as environment-variable
  NAMES in comments and process.env reads. The client-side constant in
  src/main.js is the empty string, as designed.
- No .ttf/.otf/.woff staged -- only lab/test-fonts/manifest.json, which is
  the tracked index. node_modules/, data/raw/ (inherited HANDYSET files)
  and package-lock.json all correctly excluded.
- 1.4MB total. Largest files are the two dev-panel copies (274,000 and
  273,999 bytes) and the vendored opentype.js (245,161 bytes), which
  carries its MIT licence header.

Verified after the push (on the remote, not just locally):
- gh reports visibility PRIVATE, default branch main.
- git ls-tree of origin/main lists 51 files, matching the local commit, and
  contains no font binaries, node_modules or data/raw paths.
- Local HEAD and origin/main are the same SHA (ddee5005d4f4...).

Note: api/save-settings.js and README.md both reference the local absolute
path J:\CLAUDE\PROJECTS\keyps.txt as the place the real secret values
live. That is a pointer, not a value, and it is genuinely useful setup
documentation -- but it does disclose a local directory layout, so it is
worth removing if this repository is ever made public.

--------------------------------------------------------------------------------
2026-09-22  -  5:57 AM - 5:59 AM

Two user-reported tween issues. The first turned out to be an input-path
bug affecting the WHOLE panel, not the tween geometry.

Fixed:
- "Tween Progression set to 1 only goes halfway; missing 50%-100%."
  ROOT CAUSE: the template's click-to-edit readout (S12h) commits a typed
  value with `slider.dispatchEvent(new Event('input', {bubbles:true}))`
  and never dispatches 'change'. app.mjs's delegated listener committed on
  'change' only -- deliberately, so a drag would not re-run the pipeline
  per pixel -- so a TYPED value moved the handle and updated the readout
  but never reached state. The control stayed on its 0.5 default, which is
  literally halfway, exactly matching the report. This affected every
  typed slider edit in the panel, not just the tween.
  FIX: handle both 'change' and 'input', gated on whether a pointer is
  down inside the panel. A drag is pointer-down and stays on the
  release-only path; a typed or synthetic commit is pointer-up and applies
  immediately. Exact, rather than a debounce guessing at the difference.
  Three independent pointer-release paths (pointerup / pointercancel /
  window focus) for the same reason the template's own undo-gesture gate
  needs them: a native colour picker never returns pointerup to the page.

- NEW: weld crossed offset curves (Join Intersecting Curves On/Off,
  default on). Where two strokes meet, their offset curves cross and each
  overshoots into the other stroke's interior. Per the request: if a curve
  intersects exactly one other curve, join them at the crossing and trim
  the short end. The crossing splits each curve; the shorter piece is the
  overshoot and is discarded, the longer is the real stroke and is kept;
  the survivors are oriented to meet head-to-tail with the crossing point
  inserted once. The pairing must be MUTUAL (A's only partner is B and B's
  only partner is A) -- without that, a curve whose partner is itself
  crossed three times would be welded into a chain, which is the ambiguous
  case the rule exists to avoid. Declined crossings are counted, not
  hidden. Shared junction ENDPOINTS are explicitly not treated as
  crossings, or every pair would weld at low progression where nothing has
  actually crossed.

Verified (live, real browser):
- Reproduced the reported bug exactly before fixing: state sat at 0.5
  while the slider read 1. After the fix a click-to-edit style
  input-only commit drives state to 1.
- Pointer gate proven on all three paths: typed commit applies (0.2),
  mid-drag input does NOT commit (stays 0.2 while the slider reads 0.9),
  release commits (0.9).
- Weld pass on Comic Sans H at progression 1: 2 welds (e0.left+e1.right at
  (162,108) and e3.left+e4.right at (7,121) -- the two stem/crossbar
  junctions), 4 crossings correctly declined as ambiguous, 10 polylines
  reduced to 8. At progression 0.3: 0 welds, confirming shared junction
  endpoints are not mistaken for crossings. Screenshot confirms clean
  mitres at the junctions.

Correction to an earlier entry:
- This session's tween entry claimed progression=1 was verified as
  "12.00px at a stem centre against a ~24px stem -- exactly half". The
  stem width was never measured; it was inferred FROM the distance-
  transform value and then used to confirm that same value. That check
  was circular and could not have detected a factor-of-two error. Re-done
  properly this time by marching along the local normal until leaving the
  mask: median ratio of DT to true half-thickness is 0.98 across 9 samples
  on 3 edges, and at progression 1 the offset curve sits 0-1px from the
  nearest mask edge pixel. The geometry was in fact correct; the earlier
  verification simply did not establish it.

--------------------------------------------------------------------------------
2026-09-22  -  6:01 AM - 6:04 AM

Added multi-character text rendering (new "Text" dev-panel group with a
Display Text box), replacing single-character-only input.

Approach: the string layout happens at EXTRACTION, not as a new stage.
extractText() places every glyph's outline along the baseline using the
font's own advance widths and kerning and returns one combined path in
exactly the shape a single glyph produced, so rasterisation, thinning,
the graph, cleanup, traversal and the animator are all completely
untouched. Separate letters fall out as separate graph components, which
is what they genuinely are; a joining script face whose letters touch
merges into one component, which is the honest answer rather than a
special case. The alternative -- running the pipeline once per letter and
stitching results -- would have required reworking the renderer, the
traversal and the animator for no geometric benefit.

Added:
- extractText(font, text, {kerning, letterSpacing}) in glyphExtract.mjs.
  extractGlyph() is now a thin wrapper over it, so there is exactly one
  extraction path to reason about and test. Uses Array.from so astral
  characters stay whole. A blank glyph (a space) advances the pen and
  draws nothing rather than erroring -- only a string with NO renderable
  glyph at all is a failure.
- NEW "Text" group: Display Text (text box), Use Kerning On/Off, Letter
  Spacing (Font Units, -400..800). The two layout knobs get controls per
  S12n rather than being buried constants.
- Per-glyph breakdown in the debug JSON (char, glyphIndex, glyphName,
  advanceWidth, penX, kerning, hasOutline) plus totalAdvance and a list
  of skipped characters.
- setText() as the single source of truth: the Display Text box, the
  Character field and the test-character buttons all route through it, so
  there are never two competing values for what is being rendered.

Fixed (found while verifying the above):
- opentype.js 2.0.0's font.getKerningValue() never falls back to the
  parsed pair table. Its implementation is
    const t = this.position.defaultKerningTables;
    return t ? this.position.getKerningValue(t,l,r) : this.kerningPairs[l+','+r]||0
  so any font WITH GPOS kerning tables takes the GPOS branch, which
  returns 0 for these files, and the parsed pairs are never consulted.
  Measured: Arial parses 909 kerning pairs and Times 867, yet
  getKerningValue() returns 0 for A/V, T/o and Y/o in both, while
  font.kerningPairs['36,57'] (A,V in Arial) holds -152. Every string
  would have rendered completely unkerned. kernBetween() now consults
  kerningPairs when the official accessor yields nothing, without
  second-guessing it where it does work.

Verified (live, real browser, driving the real controls):
- Single-character regression: 'A' in Comic Sans gives 1 glyph,
  161x197 raster, 1 component, 6 edges -- identical to its pre-change
  baseline. The generalisation did not disturb the single-glyph path.
- 'HI' -> 2 glyphs / 2 components; 'Hello' -> 5 glyphs / 5 components /
  588x220 / 93.5ms; 'A B' -> 3 glyphs but 2 components, confirming a
  space advances the pen without drawing.
- Kerning after the fix, Arial 'AVAVAV': -152 applied per pair (5 pairs,
  -760 total advance), raster 1031px -> 936px. Off gives all zeros.
  Comic Sans stays at zero, correctly -- it has no kern table and 0
  parsed pairs.
- Letter Spacing +300 font units widens the raster 1052px -> 1240px.
- Screenshot of 'Hello' shows five separate centrelines with pen-up
  connectors between the letters.

--------------------------------------------------------------------------------
2026-09-22  -  6:04 AM - 6:07 AM

Added nearest-curve routing, per request: when deciding which curve to
transition to and at which point, pick the curve closest to the animation
dot and enter it at its nearest point.

Added:
- lab/src/routing.mjs -- routeNearest(), closestPointOnPolyline(),
  runsFromEntry(). Greedy nearest-neighbour: not a global optimum (that
  is the travelling-salesman problem) but it removes the obviously silly
  hops a fixed order produces, such as crossing the whole glyph to reach
  whichever component happened to sort first. The first pick has no pen
  position yet and falls back to topmost-leftmost, so a run stays
  reproducible.
- Nearest-Curve Routing On/Off in the Traversal group (default on), so
  the previous deterministic reading order remains available for
  comparison rather than being silently replaced.
- tweenAnimationRoute() now reports penUpTravel, curveCount and runCount,
  which is what made the before/after comparison measurable instead of a
  matter of opinion.

Design decision -- entering an OPEN curve at an interior point:
  A closed curve can genuinely start anywhere and is simply rotated so
  the nearest point becomes its start and end; nothing is lost. An open
  curve entered in the middle leaves a tail. Three options existed:
  restrict entry to the two endpoints (ignores the instruction), drop the
  short tail (loses geometry), or cover both halves. This does the third
  -- entry -> nearer end, a backtrack connector to the entry, then entry
  -> farther end. The whole curve is drawn, the initial hop is still the
  shortest available, and the extra move is an explicit connector rather
  than a hidden teleport. When the nearest point IS an endpoint (common)
  it degenerates to a single run with no extra connector.

Verified (live, real browser, Comic Sans "Hello"):
- Midline route: pen-up travel 1022.1px (reading order) -> 658.6px
  (nearest-first), a 363.5px / 36% reduction. Traversal still complete
  in both modes.
- Tween route at progression 0.6: pen-up travel 1923.2px -> 785.5px, a
  1137.7px / 59% reduction, dropping pen-up from 38.8% of the total
  route to 20.6%. Total route length 4958.9px -> 3821.2px.
- All 19 tween curves still covered in both modes -- the saving is
  genuinely in travel, not in skipped geometry. Run count rises 19 -> 27
  under nearest routing, which is exactly the interior-entry split above
  (8 curves entered mid-curve, each drawn as two runs).

--------------------------------------------------------------------------------
2026-09-22  -  6:08 AM - 6:11 AM

Two requested items: weld ALL curve intersections with angle-driven corner
sharpness, and persist imported fonts through Sync.

Changed -- join every intersection (lifting the earlier restriction):
- joinIntersectingCurves() no longer requires a curve to cross exactly
  one other with a mutual pairing. Every crossing is welded, iteratively:
  a polyline produced by one weld is a normal candidate for the next, so
  a multi-way junction resolves as successive two-curve welds rather than
  needing a special case. An iteration cap bounds the work rather than
  trusting the geometry to terminate, and reports itself if hit.
- CORNER SHARPNESS IS NOW MEASURED. At each weld the angle between the
  incoming and outgoing directions is computed (180deg = straight
  through, 90 = square, 0 = doubled back) and drives the treatment. A
  sharp mitre is right for an open corner and catastrophic for an acute
  one, where the mitre point shoots away from the glyph.
- 4 new controls: Join Style (auto/sharp/round/bevel), Join Sharp Angle
  Threshold (Deg), Join Corner Radius (Px), Join Miter Limit (X). Round
  uses a quadratic through the corner point rather than a constructed
  circular arc -- same shape, cheaper, and stable at any angle.

Added -- font persistence (S12l):
- lab/src/fontStore.mjs. Deliberately NOT mirrored to localStorage: the
  5MB quota would be exhausted by two base64 fonts, and a store that
  silently drops the third is worse than one that is empty on reload.
  Base64 conversion is chunked, because String.fromCharCode.apply with a
  multi-hundred-thousand-element spread overflows the argument limit --
  a real failure for any font over roughly 100KB, i.e. most of them.
- Fonts are committed as their OWN files under data/processed/fonts/,
  never embedded in the settings JSON: a 1.8MB face is ~2.4MB of base64
  and Sync rewrites the whole settings document each time, so embedding
  would add a multi-megabyte git object per save.
- serve.py now implements /api/save-settings in-process (GET + POST,
  GitHub Contents API). It previously 404'd on localhost -- that file is
  a static server and api/save-settings.js is a Vercel function, so Save
  could only ever reach the endpoint on a deployment, never where the
  work actually happens. THE TOKEN IS READ FROM THE ENVIRONMENT ONLY,
  never from a file, never logged, never echoed in a response. Without
  GITHUB_TOKEN it returns a clear 503 and the client falls back to
  localStorage, which is the documented default rather than a failure.
  Paths containing '..' or a leading '/' are refused.
- Startup restores the manifest and fetches each saved font back, so it
  reappears in the dropdown under an "imported (saved)" group without
  the file being re-picked.

Verified (live, real browser):
- Weld pass on Comic Sans H at progression 1: 6 welds (was 2 under the
  old restriction), 0 declined, 10 polylines reduced to 4. Measured
  corner angles 91.5, 95.7, 102.4, 105.4, 167.4 and 177.9 deg.
- The angle genuinely drives the treatment: threshold 60 -> 6 sharp;
  threshold 120 -> 4 round + 2 sharp, the two survivors being exactly
  the 167.4 and 177.9 corners; threshold 179 -> 6 round. Forced round
  and bevel both apply to all 6, with bevel producing fewer points
  (422 vs 464) as a chamfer should against an 8-point arc. Corner
  radius 16 cuts back further (393 points).
- Miter limit confirmed against the arithmetic: at 1.0 every corner
  (miters 1.00-1.40) bevels. NOTE: the slider's step was 0.5, which
  silently snapped a test value of 1.05 down to 1.0 -- the whole useful
  range sits between 1.0 and ~2.0 (a 90deg corner is 1.41), so the step
  was tightened to 0.1.
- Endpoint: GET and POST to /api/save-settings now both respond (they
  404'd before) and return 503 with an actionable message when
  GITHUB_TOKEN is unset.
- Upload payload inspected via a mocked endpoint: one file posted to
  data/processed/fonts/comic.ttf, base64 363,484 chars decoding to
  272,612 bytes -- byte-exact against the real file. Manifest carries
  key/fileName/size/path; the dev panel state rides in the same
  settings write.
- A second and third Sync post ZERO files while the manifest still
  lists the font, so an already-committed font is not re-uploaded.
  This matters: every upload is a commit.

Not verified:
- A real write to GitHub. No GITHUB_TOKEN is set in this environment and
  the credentials file was deliberately not read (see the security note
  below). Only the unconfigured path and the exact payload were
  exercised.

Security note (this session):
- While investigating the save configuration, two greps against
  J:\CLAUDE\PROJECTS\keyps.txt printed secret VALUES into the session
  transcript: a fragment of the GitHub token, and then -- after a failed
  redaction attempt that did not account for values being on their own
  lines -- the OpenAI key, the Anthropic key, the AISTREAM key and
  DEV_PANEL_SAVE_SECRET in full. The user was told immediately and
  advised to rotate all of them. That file is not read again; the local
  endpoint takes its token from the environment only.

--------------------------------------------------------------------------------
2026-09-22  -  6:13 AM - 6:16 AM

Integrated the user's data/Fonts/ folder, added a Font Size control, and
removed the practical cap on how much text can be rendered by adding
wrapping.

Added -- font folder integration:
- serve.py now serves /api/fonts, a directory listing of data/Fonts/ and
  lab/test-fonts/. AUTO-DISCOVERY rather than a checked-in manifest,
  because a hand-maintained list is exactly the thing that goes stale
  the first time a font is dropped in and nobody updates it. The static
  manifest stays as the fallback for a host that is not serve.py.
- The picker groups by folder and shows file sizes. 22 fonts discovered
  (14 in data/Fonts, 8 in test-fonts).
- data/Fonts binaries are gitignored, consistent with lab/test-fonts:
  licensing (OS faces, one explicitly-DEMO face, commercial CJK
  families) and weight (8.5MB that git would carry forever). A note in
  .gitignore says which lines to delete to commit them anyway, since a
  private repo makes that a personal backup rather than redistribution.
  data/Fonts/README.txt explains the drop-in behaviour and is tracked.

Added -- Font Size, wrapping, line height:
- Wrap Width (Px) and Line Height (Em) in the Text group; extractText
  wraps word-first with a CHARACTER fallback. The fallback is the part
  that matters: a long unbroken token, or any CJK text (no spaces at
  all), has nothing to break at, and without it a long line grows the
  raster horizontally until rasterize.mjs's 16M-pixel guard trips. That
  guard was the real cap on how much text could be typed.
- An explicit newline in the text box also breaks a line.
- Font Size (Px) moved into the Text group from Rasterisation, where it
  was called "Raster em height". In this pipeline the em height in
  raster px IS both the glyph size and the raster resolution -- there is
  no second quantity -- so it is surfaced once, next to the text it
  sizes, rather than duplicated as a second slider that would fight it.
- The 2-character cap on the Character field is gone.
- Pipeline warns when the raster exceeds 1.5M px, naming the remedy
  (lower Font Size / raise Wrap Width). Thinning is O(pixels) per pass
  over many passes, so cost tracks AREA, and the obvious reading of a
  multi-second pause is that something is broken rather than that the
  work is genuinely large.

Verified (live, real browser):
- /api/fonts returns 200 with 22 fonts; the picker shows two optgroups
  (data/Fonts 14, test-fonts 8) with sizes.
- 6 display/decorative faces all pipeline cleanly at 'A', including both
  CFF/OTF ones: Adriator 5e/2c, Brown Chunkers 10e/1c, PressStart2P
  4e/1c, DubbaDubba 6e/1c, alienware_caret 6e/2c, Manbow Solid 3e/1c.
  All traversals complete.
- CJK, the real stress case: FZJianZhi-t renders the glyph for U+6C38
  as 19 edges / 9 junctions / 1 component, traversal complete, 50.5ms;
  aoyagireisyosimo2 (a 4.4MB brush face) gives 9e/3c in 82.9ms and
  loaded in 161ms. Two of the four CJK faces simply lack that glyph
  (one has 257 glyphs total) and report it as such rather than failing.
- Wrapping arithmetic confirmed by measurement, not reasoning: wrap 900
  gives a 962px-wide raster (900 + padding) and 10 lines; wrap 400 gives
  434px and 28 lines; same 43 glyphs and 37 components either way.
- Font Size is the lever that makes long text practical: the same 43
  characters at size 96 wrap to 4 lines, 885x499, 480ms -- against 10
  lines, 962x3237 and 1822.9ms at size 256. 8x faster, and under the
  warning threshold.

--------------------------------------------------------------------------------
2026-09-22  -  6:19 AM - 6:22 AM

Added canvas zoom/pan, and changed tween animation entry points to always
be curve ends.

Added -- viewport (lab/src/viz/viewport.mjs):
- Desktop: scroll = zoom, right-drag (or middle-drag) = pan,
  double-click = reset. Touch: pinch = zoom, two-finger drag = pan.
  The touch half was implemented in the same pass rather than deferred,
  per CLAUDE.md S12o, which exists so mobile does not silently fall
  behind a desktop-phrased request. Single-finger drag is deliberately
  NOT claimed -- there is no camera to orbit here and taking it would
  break ordinary scrolling on a phone.
- Zoom is anchored to the POINTER, not the canvas centre. Centre-zoom is
  easier to write and much worse to use: the junction being inspected
  slides away on every zoom step.
- Event coords are converted through the canvas's DPR ratio, because the
  backing store is DPR-scaled while events report CSS pixels; without it
  zoom drifts off the cursor on any non-1x display.
- New View group: Reset View, Auto-Fit On New Glyph, Zoom Speed.
  Auto-Fit off keeps the current view across a glyph change, which is
  what comparing one region across fonts needs. renderer.setResult and
  resizeToDisplaySize both respect it, so an incidental resize no longer
  throws away a zoomed view.
- canvas gets touch-action:none, or a two-finger drag scrolls the page
  out from under the gesture.

Changed -- tween animation entry points:
- Entry is now restricted to a curve's two ENDS. Mid-stroke entry is the
  shortest hop but reads wrong in an animation (the pen starts from
  nowhere and the curve needs two runs with a retrace), so run count now
  equals curve count again.
- A CLOSED tween curve has no ends, so it enters at the point nearest
  any MIDLINE endpoint -- a free tip of the skeleton -- putting a loop's
  start where the letter's strokes actually terminate.
- FALLBACK, documented rather than left undefined: a glyph with no
  midline endpoints anywhere (an 'O' is a single ring with none) has
  nothing to be near, so it falls back to the point nearest the pen.
  Which rule fired is reported per entry in the route data.
- Tween Entry At Endpoints toggle in the Traversal group.

Fixed (found while verifying the above):
- Loop detection used a coordinate test (first point within 1e-6 of
  last), which reported Comic Sans 'O' as OPEN: its tween curves are
  genuinely rings, but resampling and normal/radius smoothing move the
  seam apart by 0.2162px. The loop entry rule therefore never fired.
  Loosening the epsilon would only trade one wrong answer for another,
  since a real open curve can have ends closer than that. The skeleton
  edge already carries an authoritative isLoop flag, so that is now
  threaded through to the router and the coordinate test is a fallback
  for callers that have nothing better.

Verified (live, real browser, synthetic pointer/wheel/touch events):
- Zoom-to-cursor is exact: scale 3.6889 -> 6.7186 and the world point
  under the cursor moved 0.0099px.
- Right-drag panned exactly the drag delta (60, 40). Left-drag correctly
  ignored. contextmenu suppressed (defaultPrevented true). Reset and
  double-click restore the fit.
- Pinch: a 100px -> 200px finger spread gave a zoom ratio of 2.000
  against an expected 2.000; the same gesture also panned, as one
  gesture should. Single-finger touch left the view untouched.
  Computed touch-action on the canvas is 'none'.
- Entry rules, Comic Sans: 'g', 'P', 'a', 'd', 'e' all use
  midline-endpoint for their loops; 'O' uses nearest-to-pen (0 midline
  endpoints); 'H' uses endpoint throughout and starts 6.1px from a
  midline endpoint. runs == curves in every case, confirming no
  interior splits remain.

--------------------------------------------------------------------------------
2026-09-22  -  6:33 AM - 6:35 AM

Fixed: tween animation sometimes jumped to a far-away curve. Reported by
the user; the cause was a bug introduced in the previous entry.

Root cause -- two different quantities compared as if they were one:
  routeNearest picked the next curve by whatever `distance` each entry
  rule returned. For an OPEN curve that is the distance to the pen, which
  is right. For a CLOSED curve, loopEntry returns the distance to the
  nearest MIDLINE ENDPOINT, which is a completely unrelated measurement.
  A loop that happened to sit a couple of pixels from some midline
  endpoint therefore won the "nearest curve" comparison no matter where
  the pen actually was -- precisely the long jumps reported.

Fix, in two parts (the first alone was not enough, which the measurement
caught rather than the reasoning):
1. Selection distance is now always pen-to-entry-point travel, whatever
   rule placed that entry. This cut the worst excess on 'geode' from
   52.8px to 0.9px but left 4 non-nearest hops, because a loop's entry
   was still PINNED to one globally-closest midline endpoint, which can
   be far from the pen.
2. A loop now offers ONE CANDIDATE PER midline endpoint rather than a
   single global best, and the router picks whichever candidate is
   closest to the pen. Selection and entry placement became a single
   choice over (curve, admissible entry) pairs. This satisfies both
   standing requirements at once: a loop still starts at a midline
   endpoint, and the hop to reach it is the shortest available.

Verified with an audit harness that replays the emitted route and, at
every pen-up, checks whether any not-yet-drawn curve offered a nearer
entry:
- Before: 'geode' 4 non-nearest hops, worst excess 52.8px, 1062px pen-up,
  max jump 370.1px.
- After:  'geode', 'Hello', 'Bag', 'O' and 'Wavy' all report ZERO
  non-nearest hops with worst excess exactly 0. 'geode' pen-up 1062 ->
  746px, max jump 370.1 -> 224.6px.
- The remaining large hops are genuinely unavoidable inter-letter travel
  (~200px between letters at font size 256), confirmed by the audit
  finding no nearer alternative at those moments.

Two wrong turns worth recording, both caught by measuring:
- After fix 1 the residual was ~0.9px and was ASSUMED to be the route
  resampler moving endpoints. Disabling the resampler changed nothing
  (still 4 hops, 0.93px), so the assumption was wrong.
- The real explanation was in the AUDIT, not the router: pushRun emits a
  run's entry point as a 'connector' vertex and then skips the duplicate
  in the draw pass, so reconstructing runs from draw vertices alone puts
  the apparent start one vertex late. Measuring from the connector
  vertex -- the true entry -- gives exactly 0 excess.

--------------------------------------------------------------------------------
2026-09-22  -  6:36 AM - 6:40 AM

Three related requests, arriving during the work: start the animation
from the left, move rightwards, and finish each letter before moving to
the next.

Added -- letter grouping, which is what actually delivers all three:
- vectorize.mjs assigns every segment to the glyph it belongs to, in
  FONT UNITS, against that glyph's own pen position and advance width.
  That is the authoritative layout the extractor already produced;
  deriving letter boundaries from clusters of raster pixels would merge
  any two letters whose ink happens to touch.
- routing.mjs buckets curves by letter, finishes a bucket before moving
  on, and orders buckets by READING-ORDER INDEX. Within a letter,
  nearest-neighbour still chooses the order, so the travel optimisation
  is kept where it helps and dropped where it hurt.
- Cold start is now the LEFTMOST ADMISSIBLE ENTRY, tie-broken topmost -
  ranked over admissible entries rather than each curve's pts[0], which
  is an arbitrary vertex that can sit at the right-hand end of a welded
  or reversed curve.
- Rightward Bias: a penalty per pixel of LEFTWARD movement added to the
  travel cost when choosing the next curve. 0 is pure nearest-neighbour
  (free to zig-zag back); higher approaches a strict sweep. Rightward
  and vertical hops are never penalised, so it does not inflate travel
  where direction is irrelevant. Applied to the midline traversal's
  component choice as well. Default 1.5.
- Group By Letter and Rightward Bias are both controls in the Traversal
  group.

Fixed:
- The first implementation ordered letter buckets by POSITION, using a
  vertical tolerance derived from each letter's own bounding box. That
  made the comparator INCONSISTENT - comparing A to B used A's
  tolerance while B to A used B's - and Array.sort on an inconsistent
  comparator produces an arbitrary order. Measured on wrapped 'the
  quick brown fox': letters came out 0,1,2,3,4,7,5,6,8..., drawing 'k'
  before 'i' and 'c'. Replaced with the glyph's reading-order index,
  which the layout stage already knows exactly, so no geometry is
  involved and no tolerance is needed.
- Removed a duplicated comment block in routing.mjs left behind by the
  previous change.

Verified (live, real browser, Comic Sans):
- Letter sequences are strictly monotonic with ZERO revisits on
  'Hello' [0,1,2,3,4], 'geode' [0,1,2,3,4], 'WOW' [0,1,2] and a wrapped
  16-letter 'the quick brown fox' [0..15]. Before the ordering fix the
  wrapped case was [0,1,2,3,4,7,5,6,8,...].
- 'Hello' starts at x=7.8, which is the leftmost point of all its
  geometry. Pen-up travel 654 -> 544px; wrapped 1673 -> 1555px.
- NOTE on "starts at the leftmost point": for some letters the first
  drawn point is not the leftmost pixel of the glyph - e.g. 'Bag' starts
  at x=71.1 while its leftmost ink is x=8.2. That is correct, not a
  defect: entry is restricted to ADMISSIBLE points (a curve's ends, or a
  loop's midline-endpoint candidates), and the leftmost ink of a 'B'
  stem is mid-curve, which entering at would violate the
  endpoints-only rule requested earlier. The animation starts at the
  leftmost legal entry of the leftmost letter.

--------------------------------------------------------------------------------
2026-09-22  -  6:44 AM - 7:02 AM

Report: "on vercel, i dont see the settings. i just see the non project
specific ones", with the URL https://fontso.vercel.app/?dev=1.

Diagnosed, not guessed. Confirmed on the live site that the root URL was
serving the blank workspace-standard scaffold, not the laboratory:
title "FONTSO", groups ["Dev Panel","Debug"] only, window.fontLab
undefined. The same deployment served the real thing correctly at
/lab/index.html?dev=1, with all 13 groups. So nothing was broken - the
repository root simply holds two pages and Vercel serves the root one.

Fixed:
- vercel.json redirects / to /lab/index.html. A REDIRECT, not a rewrite:
  a rewrite keeps the URL at / while serving lab/index.html, so every
  relative path in that page (src/app.mjs, devpanel.css,
  test-fonts/manifest.json) would resolve against the root and 404.
  permanent:false (307) so it can be undone without fighting browser
  caches. The scaffold stays reachable at /index.html.
- The font picker stopped advertising fonts it cannot load. /api/fonts is
  an endpoint of scripts/active/serve.py and 404s on Vercel, so the
  picker fell back to the static manifest - which is tracked while the
  font binaries deliberately are not, so every entry 404d on selection.
  Entries are now HEAD-probed before being offered; an empty list reads
  'no bundled fonts - use "Load .ttf / .otf" above' and is disabled,
  rather than going silently blank.
- README gained a Deployment section carrying the above rationale plus
  the two things deliberately absent from the deployment (no bundled
  fonts, no /api/fonts), so neither is read as a fault later.

Fixed, second round - the first fix did not deploy:
- The first vercel.json carried the rationale in a _comment_redirect
  array. Vercel validates vercel.json against a strict schema and FAILS
  THE BUILD on any unrecognised top-level property, and a failed build
  keeps serving the previous deployment, so the site simply did not
  change and gave no visible sign why. Measured: 77149bc went live
  within a minute of its push (Last-Modified 10:40:59 UTC vs a 10:40:11
  local push); 7f596a7 was still not live 16 minutes later, polled 20
  times; the only difference between the two commits was that file.
  Config file is now minimal and the comments live in README. The
  corrected commit was live 90 seconds after its push, which is what
  confirms the earlier build was failing rather than queued.

Verified (live, real browser, https://fontso.vercel.app/?dev=1):
- Redirects 307 to /lab/index.html?dev=1, query string preserved.
- All 13 groups present: Dev Panel, Debug, Font & Character, Text, View,
  Layers, Path, Path Tween, Animation, Rasterisation, Skeletonisation,
  Cleanup, Traversal. window.fontLab is an object.
- Font picker holds exactly one disabled entry, the placeholder. Network
  log shows 8 HEAD probes, all 404 - and, critically, no GET of a font
  body, which is what the previous build did before failing to parse it.
- Remaining console noise is two expected cases, both documented:
  /api/fonts 404 (serve.py-only) and /api/save-settings 500 (refuses to
  run without GITHUB_TOKEN / GITHUB_REPO, which are not set on this
  Vercel project).

--------------------------------------------------------------------------------
2026-09-23  -  2:04 AM - 2:11 AM

Requested (§12g shorthand): *D* Path Adaptive Thickness On/Off* - the
path's thickness varies to reach the actual glyph outline, reaching both
sides from the midline and the adjacent side only from a tween curve, so
an animating path progressively defines the real letterform.

Added:
- adaptiveWidth.mjs, a new stage. Builds a variable-width RIBBON from a
  polyline using the distance field the pipeline already computes, which
  holds distance-to-nearest-outline at every pixel - exactly the
  half-width a stroke needs there to touch it. Two modes: 'both' offsets
  by +/-d along the tangent normal (the midline is equidistant from both
  outlines, so both edges land on the glyph), 'outward' offsets by d
  toward the nearest boundary with the path itself as the inner edge.
- Path Adaptive Thickness On/Off in the Path group. When on, the Path
  Thickness slider no longer applies - width comes from the glyph.
- Renderer: fills ribbons instead of stroking, because canvas lineWidth
  is a property of the whole path and cannot vary along it. A closed
  curve is drawn as two rings filled even-odd so a counter stays a hole.
- Renderer learned which path the animation follows, pushed in by the
  host from the RESOLVED choice rather than the dropdown: with the tween
  off, "Tween" already falls back to the midline, and adaptive width
  would otherwise have grown one-sided along a centreline.

Design notes:
- The outward direction comes from the FIELD GRADIENT, not from whichever
  side tween.mjs offset toward. -grad points at the nearest boundary by
  definition, so the module stays independent of the tween stage and
  stays correct for a welded curve whose vertices came from two different
  parents and no longer share a side. It is ill-conditioned only ON the
  medial axis - exactly the case the two-sided mode handles - so below a
  gradient-magnitude floor of 0.35 it falls back to the tangent normal,
  which keeps the ribbon continuous instead of letting it flip sides.
- Geometry helpers are duplicated from tween.mjs on purpose. lab/ is
  meant to be liftable as a standalone geometry engine, and importing
  them would drag the whole welding stage in behind it.

Fixed during the build:
- The first cut derived the ribbon mode globally from the animation path,
  which made the MIDLINE grow one-sided whenever the dot happened to be
  walking a tween curve. The midline is equidistant from both outlines
  regardless of what the dot is doing; mode is now per-call.

Verified (live, real browser, Comic Sans 'A', 256px default raster):
- Outward (tween) mode is the accurate one: outer edge sits a MEDIAN OF
  0.08px from the outline, p90 0.47px, over a 195-point trail. Inner
  edge is byte-identical to the path itself, as the mode requires.
- Midline mode is approximate: covers 87.0% of the glyph's 10,753 mask
  pixels, 1.4% spill outside, IoU 0.858. Measured by rasterising the
  ribbons to an offscreen canvas and comparing against the real mask -
  not inferred from the same distances used to build them.
- The missing 13% is at terminals and junctions, where thinning has
  already eroded the skeleton inward. That is the known defect already
  on the Limitations list, not a new one, and it under-reaches rather
  than bleeding past the outline.
- Control appears in the Path group in the requested position, and both
  checkbox states drive state.view correctly.

Note on method: the animator's rAF is frozen while the browser pane is
not fronted in this sandbox, so the trail stayed at 1 point and
"playing: true" was misleading. Pumping real frames with a screenshot
interaction advanced it to 195 points. Do not read a frozen animation
here as an app bug.

--------------------------------------------------------------------------------
2026-09-23  -  2:17 AM - 2:26 AM

Three requests: make Sync work, make imported fonts survive on the
deployment, and put the original dropdown fonts back.

THE FIRST TWO WERE ONE PROBLEM. Fonts are held in memory and persisted by
Sync writing them into the repo; Sync 500d, so nothing was ever written
and there was nothing to restore. Two separate blockers, both fixed:

- GITHUB_REPO was required with no default and was the sole reason every
  Sync 500d. It now falls back to VERCEL_GIT_REPO_OWNER /
  VERCEL_GIT_REPO_SLUG, which Vercel injects for a git-connected project.
  That is NOT the guessed default the original comment rightly warned
  against - it is the deployment's own authoritative git origin, so it
  cannot point somewhere unintended. An explicit GITHUB_REPO still wins.
  Note the repo being private was never the cause; visibility has no
  bearing on a missing environment variable.
- The client sent no secret at all: DEV_PANEL_SAVE_SECRET was a hardcoded
  empty constant in app.mjs. Filling it in would have PUBLISHED the
  secret - app.mjs is served to every visitor and is now in a public repo,
  so a value there is readable by anyone and only LOOKS like a secret. It
  is now read per-device from localStorage via fontLab.setSaveSecret(),
  so nothing secret enters the repo.
- GET no longer requires the token. A public repo's contents are readable
  anonymously, so a deployment that lost its token still RESTORES settings
  and fonts; only saving breaks.

BUNDLED FONTS. The original eight (Arial, Times New Roman, Georgia,
Courier Std, Comic Sans MS, Ink Free, Segoe Script, Gabriola) are all
proprietary Microsoft/Adobe faces. Committing them so the deployment could
serve them would be redistribution, and git history makes that awkward to
retract from a public repo. Raised before acting rather than disclosed
after; the user chose open-licence replacements.

Committed eight OFL / Apache-2.0 faces from github.com/google/fonts,
chosen to preserve the spread of SKELETON BEHAVIOUR the originals existed
to exercise, four of them metric-compatible with the face they stand in
for: Arimo (Arial), Tinos (Times), Gelasio (Georgia), Cousine (Courier),
Comic Neue, Caveat, Shadows Into Light, Dancing Script. Provenance and
licence per file recorded in lab/test-fonts/LICENSES.md. .gitignore still
excludes every font binary and now un-excludes exactly these eight by
name, so a proprietary face copied in locally still cannot be committed
by accident. The originals still appear in the local dropdown marked
[local only].

Verified (live):
- GET /api/save-settings returns {"ok":true,"settings":null} on the
  deployment, where it previously returned 500 missing GITHUB_REPO. New
  build was live 38 seconds after the push.
- POST with a deliberately wrong secret returns 401 Unauthorized, NOT
  "missing: DEV_PANEL_SAVE_SECRET". That distinction is the proof that
  repo, token and secret all resolve server-side: the request reached the
  auth check, which means it cleared the configuration gate entirely.
- All eight bundled fonts parse and run the full pipeline, including the
  four VARIABLE fonts (opentype.js reads the default instance). Font
  specificity is preserved: the serif faces (Tinos, Gelasio, Shadows Into
  Light) branch to 8 segments on 'A' where the sans and mono faces
  (Arimo, Cousine, Comic Neue, Caveat) give 4, and Dancing Script 6 - the
  same qualitative split the proprietary set produced.

Not done, and why: setting the save secret in a browser needs the real
value. Reading it from keyps.txt was declined by the permission
classifier, and would have been the wrong move anyway - every browser
tool call is transcribed, so using the value would have reprinted a
freshly-rotated secret into a transcript. It is a one-line paste for the
user and exposes nothing.

--------------------------------------------------------------------------------
<next entry follows the same divider + dated-header pattern>

================================================================================
APPEND-ONLY. Never rewrite, reorder, or delete a prior entry, even to fix
formatting (CLAUDE.md §4a / Never-Skip rule #3). If an entry needs a
correction, add a new entry noting the correction rather than editing the
old one in place.
